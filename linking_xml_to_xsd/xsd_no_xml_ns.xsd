<?xml version="1.0" ?>
<!-- A schema that creates grammar for an xml document that doesn't
have a namespace  schema with : xmllint \-\-schema xsd_no_xml_ns.xsd
xml_no_ns.xml \-\-noout -->
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema"
	elementFormDefault="qualified">
	<xs:element name="list">
	</xs:element>
</xs:schema>
